<div id="header">
  <% if current_user %>
    <div class="name"> <h2> <%= current_user.name %> </h2> </div>
  <% end %>
    <div class="menu">
      <ul>
        <% if current_user %>
          <li > <%= link_to "Contact Lists", contact_lists_path %> </li>
          <li > <%= link_to "Settings", edit_agent_path(current_user) %> </li>
          <li class="logout"> <%= link_to "Logout", logout_path %> </li>
        <% else %>
          <li> <a href="/login">Log In</a> </li>
          <li> <a href="/register">Register</a></li>
        <% end %>
      </ul>
      <br/>
      <div class="language">  </div>
    </div>
</div>
